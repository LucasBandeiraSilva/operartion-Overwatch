spring:
  application:
    name: OW-logistic

eureka:
  client:
    service-url:
      defaultZone: 'http://localhost:8761/eureka'

  instance:
    instance-id: ${spring.application.name}:${spring.application.instance-id:${random.value}}

server:
  port: 8083

minio:
  url: 'http://localhost:9000'
  access-key: admin
  secret-key: admin123
  bucket-name: overwatch.supers
